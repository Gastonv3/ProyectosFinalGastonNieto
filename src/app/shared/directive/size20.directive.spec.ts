import { Size20Directive } from './size20.directive';

describe('Size20Directive', () => {
  it('should create an instance', () => {
    const directive = new Size20Directive();
    expect(directive).toBeTruthy();
  });
});
